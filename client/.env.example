# VITE_API_URL is only needed when the frontend is hosted separately from the backend.
# If using unified hosting (server serves the built React app), leave this unset.
# VITE_API_URL=https://your-api-server.com/api
